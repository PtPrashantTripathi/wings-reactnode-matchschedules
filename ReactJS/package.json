{
  "name": "match_schedule",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "*",
    "jest-junit-reporter": "*",
    "react": "*",
    "react-dom": "*",
    "react-scripts": "*",
    "react-test-renderer": "*",
    "web-vitals": "*",
    "xml-js": "*",
    "xml2js": "*"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "rm -rf ./test-report.xml && CI=true ./node_modules/.bin/react-scripts test --no-watchman --verbose --env=jsdom --testResultsProcessor ./node_modules/jest-junit-reporter",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
